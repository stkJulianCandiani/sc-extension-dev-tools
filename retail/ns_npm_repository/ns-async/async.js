const waterfall = require('./src/waterfall');
const eachSeries = require('./src/eachSeries');
const each = require('./src/each');
const series = require('./src/series');
const parallel = require('./src/parallel');
const doUntil = require('./src/doUntil');

module.exports = { waterfall, eachSeries, each, series, parallel, doUntil };
